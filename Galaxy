getgenv().ToolsCreator = true

local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local HttpService = game:GetService("HttpService")
local Player = Players.LocalPlayer

local SavedTools = getgenv().TC_Saves or {}
getgenv().TC_Saves = SavedTools
local ActiveTools = {}
local Connections = {}
local ToolCooldowns = {}
local ToolStats = {}
local Favorites = getgenv().TC_Favs or {}
getgenv().TC_Favs = Favorites
local UndoHistory = {}
local Keybinds = getgenv().TC_Keybinds or {}
getgenv().TC_Keybinds = Keybinds
local Macros = getgenv().TC_Macros or {}
getgenv().TC_Macros = Macros
local ToolGroups = getgenv().TC_Groups or {}
getgenv().TC_Groups = ToolGroups
local Profiles = getgenv().TC_Profiles or {}
getgenv().TC_Profiles = Profiles
local Snippets = getgenv().TC_Snippets or {}
getgenv().TC_Snippets = Snippets
local LogEntries = {}
local ToolTimers = {}
local ComboTracker = {}
local ToolConditions = {}
local ToolEvents = {}
local ToolChains = {}
local ToolVars = getgenv().TC_Vars or {}
getgenv().TC_Vars = ToolVars
local ToolSchedules = {}
local ToolInstances = {}
local ToolCdConns = {}
local autoRespawnTools = true
local logEnabled = true
local minimized = false
local guiOpen = false
local cdOverlayEnabled = true
local particleEnabled = true
local antiDetect = false
local currentProfile = "Default"
local cdNamingEnabled = true
local autoClearDupes = true
local toolCreationCount = 0
local sessionStart = tick()

if Player:FindFirstChild("PlayerGui") and Player.PlayerGui:FindFirstChild("ToolsCreatorGui") then
	Player.PlayerGui:FindFirstChild("ToolsCreatorGui"):Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "ToolsCreatorGui"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
if syn and syn.protect_gui then syn.protect_gui(ScreenGui) end
ScreenGui.Parent = Player:WaitForChild("PlayerGui")

local function Tween(obj, props, dur)
	TweenService:Create(obj, TweenInfo.new(dur or 0.25, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), props):Play()
end

local function MakeDraggable(frame, handle)
	handle = handle or frame
	local dragging, dragInput, dragStart, startPos
	handle.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
			dragging = true
			dragStart = input.Position
			startPos = frame.Position
			input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then dragging = false end
			end)
		end
	end)
	handle.InputChanged:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseMovement then
			dragInput = input
		end
	end)
	UIS.InputChanged:Connect(function(input)
		if input == dragInput and dragging then
			local delta = input.Position - dragStart
			frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
		end
	end)
end

local function AddLog(e)
	if not logEnabled then return end
	table.insert(LogEntries, 1, os.date("[%H:%M:%S] ") .. e)
	if #LogEntries > 100 then table.remove(LogEntries) end
end

local function ClearDuplicateTools(toolName)
	local bp = Player:FindFirstChild("Backpack")
	local char = Player.Character
	local myTool = ToolInstances[toolName]
	if bp then
		for _, t in pairs(bp:GetChildren()) do
			if t:IsA("Tool") and t.Name == toolName and t ~= myTool then
				t:Destroy()
				AddLog("Dupe removed (BP): " .. toolName)
			end
		end
	end
	if char then
		for _, t in pairs(char:GetChildren()) do
			if t:IsA("Tool") and t.Name == toolName and t ~= myTool then
				t:Destroy()
				AddLog("Dupe removed (Char): " .. toolName)
			end
		end
	end
end

local function ScanAndClearAllDupes()
	if not autoClearDupes then return end
	for tn, myTool in pairs(ToolInstances) do
		if myTool and myTool.Parent then
			ClearDuplicateTools(tn)
		end
	end
	local bp = Player:FindFirstChild("Backpack")
	local char = Player.Character
	if bp then
		local seen = {}
		for _, t in pairs(bp:GetChildren()) do
			if t:IsA("Tool") then
				if not ActiveTools[t.Name] then
					if seen[t.Name] then
					else
						seen[t.Name] = true
					end
				end
			end
		end
	end
end

local function FindToolInstance(toolName)
	local bp = Player:FindFirstChild("Backpack")
	local char = Player.Character
	if bp then
		local t = bp:FindFirstChild(toolName)
		if t and t:IsA("Tool") then return t end
	end
	if char then
		local t = char:FindFirstChild(toolName)
		if t and t:IsA("Tool") then return t end
	end
	return nil
end

local function UpdateToolCdName(toolName, remaining)
	if not cdNamingEnabled then return end
	local tool = FindToolInstance(toolName)
	if not tool then return end
	if remaining and remaining > 0 then
		tool.Name = toolName .. " [" .. string.format("%.1f", remaining) .. "s]"
	else
		tool.Name = toolName
	end
end

local NotifQueue = {}
local NotifActive = false

local function Notify(msg, color)
	table.insert(NotifQueue, {msg = msg, color = color or Color3.fromRGB(130, 70, 220)})
	if NotifActive then return end
	NotifActive = true
	task.spawn(function()
		while #NotifQueue > 0 do
			local d = table.remove(NotifQueue, 1)
			local nf = Instance.new("Frame")
			nf.Size = UDim2.new(0, 180, 0, 36)
			nf.Position = UDim2.new(0.5, -90, 1, 5)
			nf.BackgroundColor3 = Color3.fromRGB(8, 2, 22)
			nf.BorderSizePixel = 0
			nf.ZIndex = 200
			nf.Parent = ScreenGui
			Instance.new("UICorner", nf).CornerRadius = UDim.new(0, 9)
			local ns = Instance.new("UIStroke", nf)
			ns.Color = d.color
			ns.Thickness = 1.3
			ns.Transparency = 0.2
			local ng = Instance.new("UIGradient", nf)
			ng.Color = ColorSequence.new({
				ColorSequenceKeypoint.new(0, Color3.fromRGB(14, 4, 32)),
				ColorSequenceKeypoint.new(1, Color3.fromRGB(6, 1, 16))
			})
			ng.Rotation = 45
			local nl = Instance.new("TextLabel", nf)
			nl.Size = UDim2.new(1, -10, 1, 0)
			nl.Position = UDim2.new(0, 5, 0, 0)
			nl.BackgroundTransparency = 1
			nl.Text = msg
			nl.TextColor3 = Color3.fromRGB(230, 215, 255)
			nl.TextSize = 9
			nl.Font = Enum.Font.GothamBold
			nl.TextWrapped = true
			nl.ZIndex = 201
			if particleEnabled then
				for i = 1, 4 do
					local p = Instance.new("Frame", nf)
					p.Size = UDim2.new(0, 2, 0, 2)
					p.Position = UDim2.new(math.random(10, 90)/100, 0, math.random(20, 80)/100, 0)
					p.BackgroundColor3 = d.color
					p.BackgroundTransparency = 0.3
					p.BorderSizePixel = 0
					p.ZIndex = 202
					Instance.new("UICorner", p).CornerRadius = UDim.new(1, 0)
					task.spawn(function()
						Tween(p, {BackgroundTransparency = 1, Position = UDim2.new(p.Position.X.Scale + math.random(-20, 20)/100, 0, -0.4, 0)}, 1.3)
					end)
				end
			end
			Tween(nf, {Position = UDim2.new(0.5, -90, 1, -52)}, 0.3)
			task.wait(1.5)
			Tween(nf, {Position = UDim2.new(0.5, -90, 1, 10), BackgroundTransparency = 1}, 0.35)
			Tween(nl, {TextTransparency = 1}, 0.35)
			Tween(ns, {Transparency = 1}, 0.35)
			task.wait(0.4)
			nf:Destroy()
		end
		NotifActive = false
	end)
end

local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Size = UDim2.new(0, 32, 0, 32)
ToggleBtn.Position = UDim2.new(1, -40, 0, 8)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(8, 2, 20)
ToggleBtn.Text = "★"
ToggleBtn.TextColor3 = Color3.fromRGB(180, 130, 255)
ToggleBtn.TextSize = 14
ToggleBtn.Font = Enum.Font.GothamBold
ToggleBtn.BorderSizePixel = 0
ToggleBtn.ZIndex = 100
ToggleBtn.Parent = ScreenGui
Instance.new("UICorner", ToggleBtn).CornerRadius = UDim.new(0, 10)
local tgS = Instance.new("UIStroke", ToggleBtn)
tgS.Color = Color3.fromRGB(120, 60, 200)
tgS.Thickness = 1.4
MakeDraggable(ToggleBtn)

local CdOverlay = Instance.new("Frame")
CdOverlay.Size = UDim2.new(0, 100, 0, 20)
CdOverlay.Position = UDim2.new(0.5, -50, 0, 2)
CdOverlay.BackgroundColor3 = Color3.fromRGB(6, 1, 16)
CdOverlay.BackgroundTransparency = 0.2
CdOverlay.BorderSizePixel = 0
CdOverlay.ZIndex = 150
CdOverlay.Visible = false
CdOverlay.Parent = ScreenGui
Instance.new("UICorner", CdOverlay).CornerRadius = UDim.new(0, 7)
local cdStroke = Instance.new("UIStroke", CdOverlay)
cdStroke.Color = Color3.fromRGB(80, 40, 160)
cdStroke.Thickness = 1

local CdBar = Instance.new("Frame")
CdBar.Size = UDim2.new(0, 0, 1, 0)
CdBar.BackgroundColor3 = Color3.fromRGB(120, 60, 220)
CdBar.BorderSizePixel = 0
CdBar.ZIndex = 151
CdBar.Parent = CdOverlay
Instance.new("UICorner", CdBar).CornerRadius = UDim.new(0, 7)
local cdBarGrad = Instance.new("UIGradient", CdBar)
cdBarGrad.Color = ColorSequence.new({
	ColorSequenceKeypoint.new(0, Color3.fromRGB(140, 80, 240)),
	ColorSequenceKeypoint.new(1, Color3.fromRGB(80, 40, 180))
})

local CdLabel = Instance.new("TextLabel")
CdLabel.Size = UDim2.new(1, 0, 1, 0)
CdLabel.BackgroundTransparency = 1
CdLabel.Text = ""
CdLabel.TextColor3 = Color3.fromRGB(230, 210, 255)
CdLabel.TextSize = 8
CdLabel.Font = Enum.Font.GothamBold
CdLabel.ZIndex = 152
CdLabel.Parent = CdOverlay

task.spawn(function()
	local h = 0
	while ScreenGui.Parent do
		h = (h + 0.002) % 1
		tgS.Color = Color3.fromHSV(h, 0.55, 0.9)
		task.wait(0.05)
	end
end)

local fullSize = UDim2.new(0, 248, 0, 370)
local miniSize = UDim2.new(0, 248, 0, 24)

local MainFrame = Instance.new("Frame")
MainFrame.Size = fullSize
MainFrame.Position = UDim2.new(0.5, -124, 0.5, -185)
MainFrame.BackgroundColor3 = Color3.fromRGB(4, 1, 12)
MainFrame.BorderSizePixel = 0
MainFrame.Visible = false
MainFrame.ZIndex = 50
MainFrame.ClipsDescendants = true
MainFrame.Parent = ScreenGui
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 12)
local mStroke = Instance.new("UIStroke", MainFrame)
mStroke.Color = Color3.fromRGB(90, 45, 170)
mStroke.Thickness = 1.6

local GradBG = Instance.new("Frame")
GradBG.Size = UDim2.new(1, 0, 1, 0)
GradBG.BackgroundColor3 = Color3.fromRGB(5, 1, 15)
GradBG.BorderSizePixel = 0
GradBG.ZIndex = 50
GradBG.Parent = MainFrame
Instance.new("UICorner", GradBG).CornerRadius = UDim.new(0, 12)
local grad = Instance.new("UIGradient", GradBG)
grad.Color = ColorSequence.new({
	ColorSequenceKeypoint.new(0, Color3.fromRGB(7, 2, 22)),
	ColorSequenceKeypoint.new(0.35, Color3.fromRGB(2, 0, 8)),
	ColorSequenceKeypoint.new(0.65, Color3.fromRGB(9, 3, 26)),
	ColorSequenceKeypoint.new(1, Color3.fromRGB(4, 1, 14))
})

task.spawn(function()
	local r = 0
	while MainFrame.Parent do r = (r + 0.25) % 360; grad.Rotation = r; task.wait(0.04) end
end)

task.spawn(function()
	local h = 0
	while MainFrame.Parent do h = (h + 0.001) % 1; mStroke.Color = Color3.fromHSV(h, 0.5, 0.72); task.wait(0.04) end
end)

for i = 1, 70 do
	local s = Instance.new("Frame")
	local sz = math.random(1, 3)
	s.Size = UDim2.new(0, sz, 0, sz)
	s.Position = UDim2.new(math.random(1, 99)/100, 0, math.random(1, 99)/100, 0)
	s.BackgroundColor3 = Color3.fromHSV(math.random(55, 85)/100, math.random(8, 45)/100, math.random(75, 100)/100)
	s.BackgroundTransparency = math.random(15, 70)/100
	s.BorderSizePixel = 0
	s.ZIndex = 51
	s.Parent = MainFrame
	Instance.new("UICorner", s).CornerRadius = UDim.new(1, 0)
	task.spawn(function()
		while s.Parent do
			Tween(s, {BackgroundTransparency = math.random(10, 90)/100, Size = UDim2.new(0, math.random(1, sz+1), 0, math.random(1, sz+1))}, math.random(5, 16)/10)
			task.wait(math.random(5, 16)/10)
		end
	end)
end

local nebC = {Color3.fromRGB(45, 12, 105), Color3.fromRGB(22, 6, 65), Color3.fromRGB(65, 22, 135), Color3.fromRGB(35, 8, 85), Color3.fromRGB(18, 35, 95), Color3.fromRGB(55, 15, 80)}
for i = 1, 6 do
	local n = Instance.new("Frame")
	n.Size = UDim2.new(0, math.random(30, 95), 0, math.random(30, 95))
	n.Position = UDim2.new(math.random(0, 80)/100, 0, math.random(0, 80)/100, 0)
	n.BackgroundColor3 = nebC[i]
	n.BackgroundTransparency = 0.91
	n.BorderSizePixel = 0
	n.ZIndex = 51
	n.Parent = MainFrame
	Instance.new("UICorner", n).CornerRadius = UDim.new(1, 0)
	task.spawn(function()
		while n.Parent do
			Tween(n, {BackgroundTransparency = math.random(87, 96)/100, Position = UDim2.new(math.random(0, 80)/100, 0, math.random(0, 80)/100, 0), BackgroundColor3 = Color3.fromHSV(math.random(60, 85)/100, math.random(30, 60)/100, math.random(20, 50)/100)}, math.random(25, 55)/10)
			task.wait(math.random(25, 55)/10)
		end
	end)
end

task.spawn(function()
	while MainFrame.Parent do
		task.wait(math.random(35, 75)/10)
		if MainFrame.Visible and particleEnabled then
			local ss = Instance.new("Frame")
			ss.Size = UDim2.new(0, math.random(15, 40), 0, 1)
			local sy = math.random(5, 95)/100
			ss.Position = UDim2.new(-0.1, 0, sy, 0)
			ss.BackgroundColor3 = Color3.fromRGB(220, 210, 255)
			ss.BackgroundTransparency = 0.2
			ss.BorderSizePixel = 0
			ss.ZIndex = 52
			ss.Rotation = math.random(-15, 15)
			ss.Parent = MainFrame
			Instance.new("UICorner", ss).CornerRadius = UDim.new(0, 1)
			local trail = Instance.new("Frame", ss)
			trail.Size = UDim2.new(0.6, 0, 1, 0)
			trail.Position = UDim2.new(0, 0, 0, 0)
			trail.BackgroundColor3 = Color3.fromRGB(180, 160, 255)
			trail.BackgroundTransparency = 0.5
			trail.BorderSizePixel = 0
			trail.ZIndex = 52
			Instance.new("UICorner", trail).CornerRadius = UDim.new(0, 1)
			Tween(ss, {Position = UDim2.new(1.1, 0, sy + math.random(-10, 10)/100, 0), BackgroundTransparency = 1}, math.random(8, 14)/10)
			task.delay(1.6, function() ss:Destroy() end)
		end
	end
end)

local TitleBar = Instance.new("Frame")
TitleBar.Size = UDim2.new(1, 0, 0, 24)
TitleBar.BackgroundColor3 = Color3.fromRGB(10, 3, 26)
TitleBar.BackgroundTransparency = 0.08
TitleBar.BorderSizePixel = 0
TitleBar.ZIndex = 55
TitleBar.Parent = MainFrame
Instance.new("UICorner", TitleBar).CornerRadius = UDim.new(0, 12)

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, -60, 1, 0)
TitleLabel.Position = UDim2.new(0, 8, 0, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "✦ Tools Creator V1.0"
TitleLabel.TextColor3 = Color3.fromRGB(215, 180, 255)
TitleLabel.TextSize = 10
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.ZIndex = 56
TitleLabel.Parent = TitleBar
MakeDraggable(MainFrame, TitleBar)

local CountLabel = Instance.new("TextLabel")
CountLabel.Size = UDim2.new(0, 32, 1, 0)
CountLabel.Position = UDim2.new(1, -58, 0, 0)
CountLabel.BackgroundTransparency = 1
CountLabel.Text = "[0]"
CountLabel.TextColor3 = Color3.fromRGB(140, 210, 140)
CountLabel.TextSize = 8
CountLabel.Font = Enum.Font.GothamBold
CountLabel.ZIndex = 56
CountLabel.Parent = TitleBar

local MinBtn = Instance.new("TextButton")
MinBtn.Size = UDim2.new(0, 18, 0, 18)
MinBtn.Position = UDim2.new(1, -22, 0, 3)
MinBtn.BackgroundColor3 = Color3.fromRGB(32, 10, 68)
MinBtn.BorderSizePixel = 0
MinBtn.Text = "−"
MinBtn.TextColor3 = Color3.fromRGB(210, 180, 255)
MinBtn.TextSize = 12
MinBtn.Font = Enum.Font.GothamBold
MinBtn.ZIndex = 57
MinBtn.Parent = TitleBar
Instance.new("UICorner", MinBtn).CornerRadius = UDim.new(0, 5)
MinBtn.MouseButton1Click:Connect(function()
	minimized = not minimized
	Tween(MainFrame, {Size = minimized and miniSize or fullSize}, 0.25)
	MinBtn.Text = minimized and "+" or "−"
end)

local TabScroll = Instance.new("ScrollingFrame")
TabScroll.Size = UDim2.new(1, -4, 0, 16)
TabScroll.Position = UDim2.new(0, 2, 0, 26)
TabScroll.BackgroundTransparency = 1
TabScroll.BorderSizePixel = 0
TabScroll.ScrollBarThickness = 0
TabScroll.ScrollingDirection = Enum.ScrollingDirection.X
TabScroll.CanvasSize = UDim2.new(0, 0, 0, 16)
TabScroll.ZIndex = 55
TabScroll.Parent = MainFrame
local TabLayout = Instance.new("UIListLayout", TabScroll)
TabLayout.FillDirection = Enum.FillDirection.Horizontal
TabLayout.Padding = UDim.new(0, 2)
TabLayout.SortOrder = Enum.SortOrder.LayoutOrder
TabLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
	TabScroll.CanvasSize = UDim2.new(0, TabLayout.AbsoluteContentSize.X + 4, 0, 16)
end)

local Pages = {}
local TabButtons = {}

local function CreatePage(name)
	local p = Instance.new("ScrollingFrame")
	p.Name = name
	p.Size = UDim2.new(1, -4, 1, -46)
	p.Position = UDim2.new(0, 2, 0, 44)
	p.BackgroundTransparency = 1
	p.BorderSizePixel = 0
	p.ScrollBarThickness = 3
	p.ScrollBarImageColor3 = Color3.fromRGB(100, 50, 190)
	p.CanvasSize = UDim2.new(0, 0, 0, 0)
	p.ZIndex = 54
	p.Visible = false
	p.Parent = MainFrame
	local l = Instance.new("UIListLayout", p)
	l.Padding = UDim.new(0, 3)
	l.SortOrder = Enum.SortOrder.LayoutOrder
	l:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
		p.CanvasSize = UDim2.new(0, 0, 0, l.AbsoluteContentSize.Y + 10)
	end)
	Pages[name] = p
	return p
end

local function CreateTab(name, icon, order)
	local t = Instance.new("TextButton")
	t.Size = UDim2.new(0, 30, 1, 0)
	t.BackgroundColor3 = Color3.fromRGB(16, 5, 36)
	t.BorderSizePixel = 0
	t.Text = icon or name:sub(1, 2)
	t.TextColor3 = Color3.fromRGB(115, 85, 175)
	t.TextSize = 7
	t.Font = Enum.Font.GothamBold
	t.ZIndex = 56
	t.LayoutOrder = order or 0
	t.Parent = TabScroll
	Instance.new("UICorner", t).CornerRadius = UDim.new(0, 4)
	TabButtons[name] = t
	t.MouseButton1Click:Connect(function()
		for _, pg in pairs(Pages) do pg.Visible = false end
		for _, c in pairs(TabScroll:GetChildren()) do
			if c:IsA("TextButton") then Tween(c, {BackgroundColor3 = Color3.fromRGB(16, 5, 36), TextColor3 = Color3.fromRGB(115, 85, 175)}, 0.1) end
		end
		Pages[name].Visible = true
		Tween(t, {BackgroundColor3 = Color3.fromRGB(48, 18, 100), TextColor3 = Color3.fromRGB(240, 220, 255)}, 0.1)
	end)
	return t
end

local function SwitchTab(name)
	for _, pg in pairs(Pages) do pg.Visible = false end
	for _, c in pairs(TabScroll:GetChildren()) do
		if c:IsA("TextButton") then Tween(c, {BackgroundColor3 = Color3.fromRGB(16, 5, 36), TextColor3 = Color3.fromRGB(115, 85, 175)}, 0.1) end
	end
	Pages[name].Visible = true
	if TabButtons[name] then Tween(TabButtons[name], {BackgroundColor3 = Color3.fromRGB(48, 18, 100), TextColor3 = Color3.fromRGB(240, 220, 255)}, 0.1) end
end

local function AL(pg, txt, ord)
	local l = Instance.new("TextLabel")
	l.Size = UDim2.new(1, 0, 0, 12)
	l.BackgroundTransparency = 1
	l.Text = txt
	l.TextColor3 = Color3.fromRGB(175, 145, 245)
	l.TextSize = 9
	l.Font = Enum.Font.GothamBold
	l.TextXAlignment = Enum.TextXAlignment.Left
	l.LayoutOrder = ord
	l.ZIndex = 55
	l.Parent = pg
	return l
end

local function AI(pg, ph, ord, multi)
	local t = Instance.new("TextBox")
	t.Size = UDim2.new(1, 0, 0, multi and 55 or 20)
	t.BackgroundColor3 = Color3.fromRGB(10, 3, 25)
	t.BorderSizePixel = 0
	t.PlaceholderText = ph
	t.PlaceholderColor3 = Color3.fromRGB(55, 38, 95)
	t.Text = ""
	t.TextColor3 = Color3.fromRGB(225, 205, 255)
	t.TextSize = multi and 8 or 9
	t.Font = multi and Enum.Font.Code or Enum.Font.Gotham
	t.ClearTextOnFocus = false
	t.LayoutOrder = ord
	t.ZIndex = 55
	t.TextWrapped = true
	t.MultiLine = multi or false
	if multi then t.TextYAlignment = Enum.TextYAlignment.Top end
	t.Parent = pg
	Instance.new("UICorner", t).CornerRadius = UDim.new(0, 5)
	local s = Instance.new("UIStroke", t)
	s.Color = Color3.fromRGB(50, 22, 105)
	s.Thickness = 1
	t.Focused:Connect(function() Tween(s, {Color = Color3.fromRGB(160, 95, 255)}, 0.12) end)
	t.FocusLost:Connect(function() Tween(s, {Color = Color3.fromRGB(50, 22, 105)}, 0.12) end)
	return t
end

local function AB(pg, txt, ord, cb, clr)
	local b = Instance.new("TextButton")
	b.Size = UDim2.new(1, 0, 0, 20)
	b.BackgroundColor3 = clr or Color3.fromRGB(25, 8, 58)
	b.BorderSizePixel = 0
	b.Text = txt
	b.TextColor3 = Color3.fromRGB(225, 195, 255)
	b.TextSize = 9
	b.Font = Enum.Font.GothamBold
	b.LayoutOrder = ord
	b.ZIndex = 55
	b.Parent = pg
	Instance.new("UICorner", b).CornerRadius = UDim.new(0, 5)
	local bs = Instance.new("UIStroke", b)
	bs.Color = Color3.fromRGB(75, 35, 155)
	bs.Thickness = 1
	b.MouseEnter:Connect(function()
		Tween(b, {BackgroundColor3 = Color3.fromRGB(52, 20, 110)}, 0.08)
		Tween(bs, {Color = Color3.fromRGB(170, 105, 255)}, 0.08)
	end)
	b.MouseLeave:Connect(function()
		Tween(b, {BackgroundColor3 = clr or Color3.fromRGB(25, 8, 58)}, 0.08)
		Tween(bs, {Color = Color3.fromRGB(75, 35, 155)}, 0.08)
	end)
	b.MouseButton1Click:Connect(cb)
	return b
end

local function AS(pg, ord)
	local f = Instance.new("Frame")
	f.Size = UDim2.new(1, 0, 0, 2)
	f.BackgroundColor3 = Color3.fromRGB(42, 18, 90)
	f.BackgroundTransparency = 0.45
	f.BorderSizePixel = 0
	f.LayoutOrder = ord
	f.ZIndex = 54
	f.Parent = pg
	Instance.new("UICorner", f).CornerRadius = UDim.new(0, 1)
end

local function AIB(pg, ord, h)
	local f = Instance.new("Frame")
	f.Size = UDim2.new(1, 0, 0, h or 55)
	f.BackgroundColor3 = Color3.fromRGB(7, 2, 19)
	f.BorderSizePixel = 0
	f.LayoutOrder = ord
	f.ZIndex = 55
	f.Parent = pg
	Instance.new("UICorner", f).CornerRadius = UDim.new(0, 5)
	Instance.new("UIStroke", f).Color = Color3.fromRGB(45, 20, 100)
	local l = Instance.new("TextLabel")
	l.Size = UDim2.new(1, -6, 1, -4)
	l.Position = UDim2.new(0, 3, 0, 2)
	l.BackgroundTransparency = 1
	l.Text = ""
	l.TextColor3 = Color3.fromRGB(165, 135, 225)
	l.TextSize = 8
	l.Font = Enum.Font.Gotham
	l.TextWrapped = true
	l.TextYAlignment = Enum.TextYAlignment.Top
	l.TextXAlignment = Enum.TextXAlignment.Left
	l.ZIndex = 56
	l.Parent = f
	return l
end

local function ATog(pg, txt, ord, def, cb)
	local h = Instance.new("Frame")
	h.Size = UDim2.new(1, 0, 0, 18)
	h.BackgroundTransparency = 1
	h.LayoutOrder = ord
	h.ZIndex = 55
	h.Parent = pg
	local lb = Instance.new("TextLabel", h)
	lb.Size = UDim2.new(1, -38, 1, 0)
	lb.BackgroundTransparency = 1
	lb.Text = txt
	lb.TextColor3 = Color3.fromRGB(175, 145, 245)
	lb.TextSize = 8
	lb.Font = Enum.Font.GothamBold
	lb.TextXAlignment = Enum.TextXAlignment.Left
	lb.ZIndex = 55
	local tb = Instance.new("TextButton", h)
	tb.Size = UDim2.new(0, 32, 0, 14)
	tb.Position = UDim2.new(1, -34, 0.5, -7)
	tb.BackgroundColor3 = def and Color3.fromRGB(45, 165, 85) or Color3.fromRGB(50, 18, 70)
	tb.BorderSizePixel = 0
	tb.Text = def and "ON" or "OFF"
	tb.TextColor3 = Color3.fromRGB(230, 230, 255)
	tb.TextSize = 7
	tb.Font = Enum.Font.GothamBold
	tb.ZIndex = 56
	Instance.new("UICorner", tb).CornerRadius = UDim.new(0, 4)
	local st = def
	tb.MouseButton1Click:Connect(function()
		st = not st
		Tween(tb, {BackgroundColor3 = st and Color3.fromRGB(45, 165, 85) or Color3.fromRGB(50, 18, 70)}, 0.12)
		tb.Text = st and "ON" or "OFF"
		if cb then cb(st) end
	end)
	return function() return st end
end

local function ADrop(pg, txt, opts, ord, cb)
	local h = Instance.new("Frame")
	h.Size = UDim2.new(1, 0, 0, 20)
	h.BackgroundTransparency = 1
	h.LayoutOrder = ord
	h.ZIndex = 55
	h.ClipsDescendants = false
	h.Parent = pg
	local lb = Instance.new("TextLabel", h)
	lb.Size = UDim2.new(0.42, 0, 1, 0)
	lb.BackgroundTransparency = 1
	lb.Text = txt
	lb.TextColor3 = Color3.fromRGB(175, 145, 245)
	lb.TextSize = 8
	lb.Font = Enum.Font.GothamBold
	lb.TextXAlignment = Enum.TextXAlignment.Left
	lb.ZIndex = 55
	local sel = opts[1] or ""
	local db = Instance.new("TextButton", h)
	db.Size = UDim2.new(0.56, 0, 1, 0)
	db.Position = UDim2.new(0.44, 0, 0, 0)
	db.BackgroundColor3 = Color3.fromRGB(18, 6, 42)
	db.BorderSizePixel = 0
	db.Text = "▼ " .. sel
	db.TextColor3 = Color3.fromRGB(205, 185, 255)
	db.TextSize = 8
	db.Font = Enum.Font.GothamBold
	db.ZIndex = 60
	Instance.new("UICorner", db).CornerRadius = UDim.new(0, 4)
	local exp = false
	local of = Instance.new("Frame", h)
	of.Size = UDim2.new(0.56, 0, 0, 0)
	of.Position = UDim2.new(0.44, 0, 1, 2)
	of.BackgroundColor3 = Color3.fromRGB(12, 4, 32)
	of.BorderSizePixel = 0
	of.ZIndex = 70
	of.ClipsDescendants = true
	of.Visible = false
	Instance.new("UICorner", of).CornerRadius = UDim.new(0, 4)
	local ol = Instance.new("UIListLayout", of)
	ol.Padding = UDim.new(0, 1)
	for _, o in ipairs(opts) do
		local ob = Instance.new("TextButton", of)
		ob.Size = UDim2.new(1, 0, 0, 16)
		ob.BackgroundColor3 = Color3.fromRGB(20, 7, 45)
		ob.BorderSizePixel = 0
		ob.Text = o
		ob.TextColor3 = Color3.fromRGB(195, 175, 245)
		ob.TextSize = 8
		ob.Font = Enum.Font.Gotham
		ob.ZIndex = 71
		ob.MouseButton1Click:Connect(function()
			sel = o; db.Text = "▼ " .. o; exp = false; of.Visible = false
			if cb then cb(o) end
		end)
	end
	db.MouseButton1Click:Connect(function()
		exp = not exp; of.Visible = exp
		of.Size = exp and UDim2.new(0.56, 0, 0, #opts * 17) or UDim2.new(0.56, 0, 0, 0)
	end)
	return function() return sel end
end

local function DestroyTool(tn)
	if Connections[tn] then Connections[tn]:Disconnect(); Connections[tn] = nil end
	if ToolEvents[tn] then
		for _, c in pairs(ToolEvents[tn]) do pcall(function() c:Disconnect() end) end
		ToolEvents[tn] = nil
	end
	if ToolSchedules[tn] then ToolSchedules[tn] = nil end
	if ToolCdConns[tn] then ToolCdConns[tn] = nil end
	ToolCooldowns[tn] = nil
	ToolStats[tn] = nil
	ToolTimers[tn] = nil
	ToolConditions[tn] = nil
	ToolChains[tn] = nil
	local bp = Player:FindFirstChild("Backpack")
	local char = Player.Character
	if bp then for _, t in pairs(bp:GetChildren()) do if t:IsA("Tool") and (t.Name == tn or t.Name:match("^" .. tn .. " %[")) then t:Destroy() end end end
	if char then for _, t in pairs(char:GetChildren()) do if t:IsA("Tool") and (t.Name == tn or t.Name:match("^" .. tn .. " %[")) then t:Destroy() end end end
	ToolInstances[tn] = nil
	ActiveTools[tn] = nil
end

local function SpawnTool(tn, cd, code, desc, cond, chain, event)
	DestroyTool(tn)
	local bp = Player:FindFirstChild("Backpack")
	if not bp then return end
	if autoClearDupes then
		for _, t in pairs(bp:GetChildren()) do
			if t:IsA("Tool") and (t.Name == tn or t.Name:match("^" .. tn .. " %[")) then t:Destroy() end
		end
		local char = Player.Character
		if char then
			for _, t in pairs(char:GetChildren()) do
				if t:IsA("Tool") and (t.Name == tn or t.Name:match("^" .. tn .. " %[")) then t:Destroy() end
			end
		end
	end
	local cooldown = tonumber(cd) or 0
	local tool = Instance.new("Tool")
	tool.Name = tn
	tool.RequiresHandle = false
	tool.CanBeDropped = true
	tool.ToolTip = (desc and desc ~= "") and desc or (tn .. " [" .. cd .. "s]")
	tool.Parent = bp
	ToolInstances[tn] = tool
	ToolCooldowns[tn] = false
	ToolStats[tn] = {uses = 0, lastUsed = "Never", errors = 0, created = os.date("%H:%M:%S"), totalTime = 0, streak = 0, bestStreak = 0}
	ToolConditions[tn] = cond or ""
	ToolChains[tn] = chain or ""
	toolCreationCount = toolCreationCount + 1
	local conn
	conn = tool.Activated:Connect(function()
		if ToolCooldowns[tn] then return end
		if cond and cond ~= "" then
			local cOk, cR = pcall(function()
				local env = setmetatable({vars = ToolVars, player = Player}, {__index = getfenv()})
				return setfenv(loadstring("return " .. cond), env)()
			end)
			if not cOk or not cR then AddLog("Cond fail: " .. tn); return end
		end
		ToolCooldowns[tn] = true
		ToolStats[tn].uses = ToolStats[tn].uses + 1
		ToolStats[tn].lastUsed = os.date("%H:%M:%S")
		ToolStats[tn].streak = ToolStats[tn].streak + 1
		if ToolStats[tn].streak > ToolStats[tn].bestStreak then ToolStats[tn].bestStreak = ToolStats[tn].streak end
		ToolTimers[tn] = {start = tick(), duration = cooldown}
		if cdOverlayEnabled then CdOverlay.Visible = true; CdLabel.Text = tn end
		local st = tick()
		local ok, err = pcall(function()
			local env = setmetatable({vars = ToolVars, player = Player, tool = tool}, {__index = getfenv()})
			setfenv(loadstring(code), env)()
		end)
		local el = tick() - st
		ToolStats[tn].totalTime = ToolStats[tn].totalTime + el
		if not ok then
			ToolStats[tn].errors = ToolStats[tn].errors + 1
			ToolStats[tn].streak = 0
			AddLog("ERR [" .. tn .. "]: " .. tostring(err))
		else
			AddLog("Use: " .. tn .. " (" .. string.format("%.3f", el) .. "s)")
		end
		table.insert(ComboTracker, 1, {name = tn, time = tick()})
		if #ComboTracker > 25 then table.remove(ComboTracker) end
		if chain and chain ~= "" then
			task.spawn(function()
				task.wait(0.1)
				for ct in chain:gmatch("[^,]+") do
					ct = ct:match("^%s*(.-)%s*$")
					if ActiveTools[ct] and ct ~= tn then
						pcall(function()
							local env = setmetatable({vars = ToolVars, player = Player}, {__index = getfenv()})
							setfenv(loadstring(ActiveTools[ct].code), env)()
						end)
						AddLog("Chain: " .. tn .. " → " .. ct)
						task.wait(0.05)
					end
				end
			end)
		end
		if cooldown > 0 then
			ToolCdConns[tn] = true
			task.spawn(function()
				local t = tick()
				while tick() - t < cooldown and ToolTimers[tn] and ToolCdConns[tn] do
					local remaining = cooldown - (tick() - t)
					local pct = (tick() - t) / cooldown
					CdBar.Size = UDim2.new(math.clamp(1 - pct, 0, 1), 0, 1, 0)
					CdBar.BackgroundColor3 = Color3.fromRGB(
						math.floor(120 + pct * 80),
						math.floor(60 - pct * 40),
						math.floor(220 - pct * 100)
					)
					if cdOverlayEnabled then CdLabel.Text = tn .. " " .. string.format("%.1f", remaining) .. "s" end
					UpdateToolCdName(tn, remaining)
					task.wait(0.05)
				end
				ToolCooldowns[tn] = false
				ToolTimers[tn] = nil
				ToolCdConns[tn] = nil
				UpdateToolCdName(tn, nil)
				if not next(ToolTimers) then CdOverlay.Visible = false end
			end)
		else
			ToolCooldowns[tn] = false
			ToolTimers[tn] = nil
			task.delay(0.4, function() if not next(ToolTimers) then CdOverlay.Visible = false end end)
		end
	end)
	Connections[tn] = conn
	if event and event ~= "" then
		ToolEvents[tn] = {}
		if event:lower():find("loop:") then
			local interval = tonumber(event:match("loop:(%d+%.?%d*)")) or 1
			ToolSchedules[tn] = true
			task.spawn(function()
				while ToolSchedules[tn] and ActiveTools[tn] do
					pcall(function()
						local env = setmetatable({vars = ToolVars, player = Player}, {__index = getfenv()})
						setfenv(loadstring(code), env)()
					end)
					task.wait(interval)
				end
			end)
		end
		if event:lower():find("heartbeat") then
			local hbC = RunService.Heartbeat:Connect(function()
				pcall(function()
					local env = setmetatable({vars = ToolVars, player = Player}, {__index = getfenv()})
					setfenv(loadstring(code), env)()
				end)
			end)
			table.insert(ToolEvents[tn], hbC)
		end
		if event:lower():find("damage") then
			task.spawn(function()
				while ActiveTools[tn] do
					local char = Player.Character
					if char then
						local hum = char:FindFirstChildOfClass("Humanoid")
						if hum then
							local hp = hum.Health
							task.wait(0.1)
							if hum.Health < hp then
								pcall(function()
									local env = setmetatable({vars = ToolVars, player = Player, damage = hp - hum.Health}, {__index = getfenv()})
									setfenv(loadstring(code), env)()
								end)
							end
						else task.wait(0.5) end
					else task.wait(0.5) end
				end
			end)
		end
	end
	ActiveTools[tn] = {name = tn, cooldown = cd, code = code, desc = desc or "", condition = cond or "", chain = chain or "", event = event or ""}
	AddLog("Spawned: " .. tn)
end

local createPage = CreatePage("Create")
local managePage = CreatePage("Manage")
local savePage = CreatePage("Saves")
local macroPage = CreatePage("Macro")
local groupPage = CreatePage("Groups")
local varPage = CreatePage("Vars")
local snippetPage = CreatePage("Snippet")
local profilePage = CreatePage("Profile")
local logPage = CreatePage("Log")
local configPage = CreatePage("Config")

CreateTab("Create", "✦", 1)
CreateTab("Manage", "⚙", 2)
CreateTab("Saves", "💾", 3)
CreateTab("Macro", "▶", 4)
CreateTab("Groups", "📁", 5)
CreateTab("Vars", "📊", 6)
CreateTab("Snippet", "✂", 7)
CreateTab("Profile", "👤", 8)
CreateTab("Log", "📋", 9)
CreateTab("Config", "⚡", 10)

AL(createPage, "Tool Name:", 1)
local nameIn = AI(createPage, "Name...", 2)
AL(createPage, "Description:", 3)
local descIn = AI(createPage, "Optional...", 4)
AL(createPage, "Cooldown (sec):", 5)
local cdIn = AI(createPage, "0", 6)
AL(createPage, "Condition:", 7)
local condIn = AI(createPage, "true", 8)
AL(createPage, "Chain (comma sep):", 9)
local chainIn = AI(createPage, "ToolA,ToolB...", 10)
AL(createPage, "Event (loop:N/heartbeat/damage):", 11)
local eventIn = AI(createPage, "Optional...", 12)
AL(createPage, "Script:", 13)
local codeIn = AI(createPage, "print('Hello!')", 14, true)
AS(createPage, 15)

AB(createPage, "✦ Create", 16, function()
	local n = nameIn.Text
	if n == "" then Notify("Enter name!", Color3.fromRGB(200, 60, 60)) return end
	table.insert(UndoHistory, {a = "create", d = ActiveTools[n] and {name = ActiveTools[n].name, cooldown = ActiveTools[n].cooldown, code = ActiveTools[n].code, desc = ActiveTools[n].desc, condition = ActiveTools[n].condition, chain = ActiveTools[n].chain, event = ActiveTools[n].event} or nil, n = n})
	SpawnTool(n, cdIn.Text, codeIn.Text, descIn.Text, condIn.Text, chainIn.Text, eventIn.Text)
	Notify("Created: " .. n, Color3.fromRGB(60, 180, 120))
end)

AB(createPage, "✦ Create & Clear", 17, function()
	local n = nameIn.Text
	if n == "" then Notify("Enter name!", Color3.fromRGB(200, 60, 60)) return end
	table.insert(UndoHistory, {a = "create", d = nil, n = n})
	SpawnTool(n, cdIn.Text, codeIn.Text, descIn.Text, condIn.Text, chainIn.Text, eventIn.Text)
	Notify("Created: " .. n, Color3.fromRGB(60, 180, 120))
	nameIn.Text = ""; cdIn.Text = ""; codeIn.Text = ""; descIn.Text = ""; condIn.Text = ""; chainIn.Text = ""; eventIn.Text = ""
end)

AB(createPage, "⚡ Test Script", 18, function()
	if codeIn.Text == "" then Notify("No script!") return end
	local ok, err = pcall(function()
		local env = setmetatable({vars = ToolVars, player = Player}, {__index = getfenv()})
		setfenv(loadstring(codeIn.Text), env)()
	end)
	Notify(ok and "Script OK!" or ("Err: " .. tostring(err):sub(1, 35)), ok and Color3.fromRGB(60, 180, 120) or Color3.fromRGB(200, 60, 60))
end)

AB(createPage, "↩ Undo", 19, function()
	if #UndoHistory == 0 then Notify("Nothing!") return end
	local last = table.remove(UndoHistory)
	if last.a == "create" then
		if last.d then SpawnTool(last.d.name, last.d.cooldown, last.d.code, last.d.desc, last.d.condition, last.d.chain, last.d.event) else DestroyTool(last.n) end
	elseif last.a == "delete" and last.d then
		SpawnTool(last.d.name, last.d.cooldown, last.d.code, last.d.desc, last.d.condition, last.d.chain, last.d.event)
	end
	Notify("Undone!", Color3.fromRGB(200, 180, 80))
end)

AS(createPage, 20)
AL(createPage, "⚡ Templates:", 21)

local TM = {
	{n="KillAura",cd="1",c=[[local me=game.Players.LocalPlayer;local ch=me.Character;if not ch then return end;local h=ch:FindFirstChild("HumanoidRootPart");if not h then return end;for _,v in pairs(game.Players:GetPlayers())do if v~=me and v.Character then local hm=v.Character:FindFirstChild("Humanoid");local t=v.Character:FindFirstChild("HumanoidRootPart");if hm and t and(t.Position-h.Position).Magnitude<15 then hm.Health=0 end end end]]},
	{n="Speed100",cd="3",c=[[local h=game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("Humanoid");if h then h.WalkSpeed=100 end]]},
	{n="Jump150",cd="2",c=[[local h=game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("Humanoid");if h then h.JumpPower=150 end]]},
	{n="TpFwd50",cd="1",c=[[local h=game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart");if h then h.CFrame=h.CFrame*CFrame.new(0,0,-50)end]]},
	{n="GodMode",cd="5",c=[[local h=game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("Humanoid");if h then h.MaxHealth=math.huge;h.Health=math.huge end]]},
	{n="Noclip",cd="1",c=[[if getgenv().noclipC then getgenv().noclipC:Disconnect();getgenv().noclipC=nil;return end;getgenv().noclipC=game:GetService("RunService").Stepped:Connect(function()local c=game.Players.LocalPlayer.Character;if c then for _,v in pairs(c:GetDescendants())do if v:IsA("BasePart")then v.CanCollide=false end end end end)]]},
	{n="Fly",cd="2",c=[[local c=game.Players.LocalPlayer.Character;if not c then return end;local h=c:FindFirstChild("HumanoidRootPart");if not h then return end;if getgenv().flyBV then getgenv().flyBV:Destroy();getgenv().flyBV=nil;return end;local bv=Instance.new("BodyVelocity",h);bv.MaxForce=Vector3.new(1e5,1e5,1e5);bv.Velocity=Vector3.new(0,50,0);getgenv().flyBV=bv]]},
	{n="Heal",cd="3",c=[[local h=game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("Humanoid");if h then h.Health=h.MaxHealth end]]},
	{n="Invis",cd="5",c=[[local c=game.Players.LocalPlayer.Character;if c then for _,v in pairs(c:GetDescendants())do if v:IsA("BasePart")or v:IsA("Decal")then v.Transparency=1 end end end]]},
	{n="Visible",cd="1",c=[[local c=game.Players.LocalPlayer.Character;if c then for _,v in pairs(c:GetDescendants())do if v:IsA("BasePart")then v.Transparency=0 end;if v:IsA("Decal")then v.Transparency=0 end end end]]},
	{n="TpMouse",cd="1",c=[[local m=game.Players.LocalPlayer:GetMouse();local h=game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart");if h and m.Hit then h.CFrame=m.Hit+Vector3.new(0,3,0)end]]},
	{n="Explode",cd="5",c=[[local h=game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart");if h then local e=Instance.new("Explosion",workspace);e.Position=h.Position;e.BlastRadius=20;e.BlastPressure=0 end]]},
	{n="Freeze",cd="3",c=[[local h=game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart");if h then h.Anchored=not h.Anchored end]]},
	{n="Gravity",cd="2",c=[[if workspace.Gravity==196.2 then workspace.Gravity=50 else workspace.Gravity=196.2 end]]},
	{n="Reset",cd="0",c=[[local h=game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("Humanoid");if h then h.WalkSpeed=16;h.JumpPower=50 end]]},
	{n="Fling",cd="3",c=[[local c=game.Players.LocalPlayer.Character;if c then local h=c:FindFirstChild("HumanoidRootPart");if h then h.Velocity=Vector3.new(math.random(-200,200),200,math.random(-200,200))end end]]},
	{n="Spin",cd="2",c=[[local h=game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart");if not h then return end;if getgenv().spinAV then getgenv().spinAV:Destroy();getgenv().spinAV=nil;return end;local av=Instance.new("BodyAngularVelocity",h);av.AngularVelocity=Vector3.new(0,30,0);av.MaxTorque=Vector3.new(0,1e5,0);getgenv().spinAV=av]]},
	{n="BigHead",cd="1",c=[[for _,v in pairs(game.Players:GetPlayers())do if v.Character and v.Character:FindFirstChild("Head")then v.Character.Head.Size=Vector3.new(5,5,5)end end]]},
}

for i, t in ipairs(TM) do
	AB(createPage, "📋 " .. t.n, 21 + i, function()
		nameIn.Text = t.n; cdIn.Text = t.cd; codeIn.Text = t.c; descIn.Text = t.n; condIn.Text = ""; chainIn.Text = ""; eventIn.Text = ""
		Notify("Template: " .. t.n)
	end)
end

AL(managePage, "Tool Name:", 1)
local delIn = AI(managePage, "Name...", 2)

AB(managePage, "🗑 Delete", 3, function()
	local n = delIn.Text; if not ActiveTools[n] then Notify("Not found!") return end
	table.insert(UndoHistory, {a = "delete", d = {name = ActiveTools[n].name, cooldown = ActiveTools[n].cooldown, code = ActiveTools[n].code, desc = ActiveTools[n].desc, condition = ActiveTools[n].condition or "", chain = ActiveTools[n].chain or "", event = ActiveTools[n].event or ""}, n = n})
	DestroyTool(n); Notify("Deleted: " .. n, Color3.fromRGB(200, 120, 60))
end)

AB(managePage, "⭐ Favorite", 4, function()
	local n = delIn.Text; if n == "" then return end
	Favorites[n] = not Favorites[n] or nil
	Notify(Favorites[n] and ("⭐ " .. n) or ("Unfav: " .. n))
end)

AB(managePage, "📊 Stats", 5, function()
	local n = delIn.Text; if not ToolStats[n] then Notify("Not found!") return end
	local s = ToolStats[n]
	local avg = s.uses > 0 and string.format("%.3f", s.totalTime / s.uses) or "N/A"
	Notify(n .. ": " .. s.uses .. "x err:" .. s.errors .. " avg:" .. avg .. "s 🔥" .. s.bestStreak, Color3.fromRGB(100, 180, 255))
end)

AB(managePage, "✏ Edit", 6, function()
	local n = delIn.Text; if not ActiveTools[n] then Notify("Not found!") return end
	local a = ActiveTools[n]
	nameIn.Text = a.name; cdIn.Text = a.cooldown; codeIn.Text = a.code; descIn.Text = a.desc; condIn.Text = a.condition or ""; chainIn.Text = a.chain or ""; eventIn.Text = a.event or ""
	SwitchTab("Create"); Notify("Editing: " .. n)
end)

AB(managePage, "📋 Duplicate", 7, function()
	local n = delIn.Text; if not ActiveTools[n] then Notify("Not found!") return end
	local nn = n .. "_copy"; local c = 1; while ActiveTools[nn] do c = c + 1; nn = n .. "_copy" .. c end
	local a = ActiveTools[n]; SpawnTool(nn, a.cooldown, a.code, a.desc, a.condition, a.chain, a.event)
	Notify("Duped: " .. nn, Color3.fromRGB(100, 200, 150))
end)

AB(managePage, "🔄 Rename", 8, function()
	local old = delIn.Text; if not ActiveTools[old] then Notify("Not found!") return end
	local nn = nameIn.Text; if nn == "" then Notify("Set name in Create!") return end
	local a = ActiveTools[old]; DestroyTool(old)
	SpawnTool(nn, a.cooldown, a.code, a.desc, a.condition, a.chain, a.event)
	Notify(old .. " → " .. nn, Color3.fromRGB(100, 200, 180))
end)

AS(managePage, 9)

AB(managePage, "🗑 Delete ALL", 10, function()
	local c = 0
	for n, v in pairs(ActiveTools) do
		table.insert(UndoHistory, {a = "delete", d = {name = v.name, cooldown = v.cooldown, code = v.code, desc = v.desc, condition = v.condition or "", chain = v.chain or "", event = v.event or ""}, n = n})
		c = c + 1
	end
	local ns = {}; for n in pairs(ActiveTools) do table.insert(ns, n) end
	for _, n in ipairs(ns) do DestroyTool(n) end
	ActiveTools = {}; Notify("Deleted " .. c, Color3.fromRGB(200, 120, 60))
end)

AB(managePage, "🔄 Re-equip All", 11, function()
	local d = {}; for _, v in pairs(ActiveTools) do table.insert(d, {name = v.name, cooldown = v.cooldown, code = v.code, desc = v.desc, condition = v.condition, chain = v.chain, event = v.event}) end
	for _, x in pairs(d) do SpawnTool(x.name, x.cooldown, x.code, x.desc, x.condition, x.chain, x.event) end
	Notify("Re-equipped " .. #d, Color3.fromRGB(100, 180, 255))
end)

AB(managePage, "⏹ Stop All Events", 12, function()
	local c = 0
	for tn, evts in pairs(ToolEvents) do for _, cn in pairs(evts) do pcall(function() cn:Disconnect() end) end; c = c + 1 end
	ToolEvents = {}; for tn in pairs(ToolSchedules) do ToolSchedules[tn] = nil end
	Notify("Stopped " .. c .. " events", Color3.fromRGB(200, 120, 60))
end)

AB(managePage, "🧹 Scan & Clear Dupes", 13, function()
	ScanAndClearAllDupes(); Notify("Dupes cleared!", Color3.fromRGB(100, 200, 180))
end)

AS(managePage, 14)
AL(managePage, "Keybind:", 15)
local kbIn = AI(managePage, "Key (E,F,G...)", 16)

AB(managePage, "🎮 Set Keybind", 17, function()
	local n = delIn.Text; local k = kbIn.Text:upper()
	if n == "" or k == "" then Notify("Fill!") return end
	Keybinds[k] = n; Notify(k .. " → " .. n, Color3.fromRGB(180, 140, 255))
end)

AB(managePage, "🎮 Remove Keybind", 18, function()
	local k = kbIn.Text:upper()
	if Keybinds[k] then Keybinds[k] = nil; Notify("Removed: " .. k) else Notify("No bind!") end
end)

AS(managePage, 19)
AL(managePage, "Active Tools:", 20)
local activeLabel = AIB(managePage, 21, 80)
AL(managePage, "Keybinds:", 22)
local kbLabel = AIB(managePage, 23, 40)

AL(savePage, "Save Slot:", 1)
local saveIn = AI(savePage, "Slot name...", 2)
AS(savePage, 3)

AB(savePage, "💾 Save All", 4, function()
	local sn = saveIn.Text; if sn == "" then Notify("Enter name!") return end
	local d = {}; for _, v in pairs(ActiveTools) do table.insert(d, {name = v.name, cooldown = v.cooldown, code = v.code, desc = v.desc, condition = v.condition, chain = v.chain, event = v.event}) end
	if #d == 0 then Notify("No tools!") return end
	SavedTools[sn] = d; Notify("Saved " .. #d .. " → " .. sn, Color3.fromRGB(60, 180, 120))
end)

AB(savePage, "📂 Load", 5, function()
	local sn = saveIn.Text; if not SavedTools[sn] then Notify("Not found!") return end
	for _, d in pairs(SavedTools[sn]) do SpawnTool(d.name, d.cooldown, d.code, d.desc, d.condition, d.chain, d.event) end
	Notify("Loaded " .. #SavedTools[sn] .. " ← " .. sn, Color3.fromRGB(100, 180, 255))
end)

AB(savePage, "📂 Merge", 6, function()
	local sn = saveIn.Text; if not SavedTools[sn] then Notify("Not found!") return end
	local c = 0; for _, d in pairs(SavedTools[sn]) do if not ActiveTools[d.name] then SpawnTool(d.name, d.cooldown, d.code, d.desc, d.condition, d.chain, d.event); c = c + 1 end end
	Notify("Merged " .. c, Color3.fromRGB(100, 200, 180))
end)

AB(savePage, "🗑 Delete Save", 7, function()
	local sn = saveIn.Text; if not SavedTools[sn] then Notify("Not found!") return end
	SavedTools[sn] = nil; Notify("Deleted: " .. sn, Color3.fromRGB(200, 120, 60))
end)

AB(savePage, "🗑 Delete ALL Saves", 8, function()
	local c = 0; for _ in pairs(SavedTools) do c = c + 1 end
	SavedTools = {}; getgenv().TC_Saves = SavedTools
	Notify("Deleted " .. c .. " saves!", Color3.fromRGB(200, 120, 60))
end)

AS(savePage, 9)
AL(savePage, "Rename:", 10)
local renIn = AI(savePage, "New name...", 11)

AB(savePage, "✏ Rename", 12, function()
	local o = saveIn.Text; local n = renIn.Text
	if o == "" or n == "" or not SavedTools[o] then Notify("Check!") return end
	SavedTools[n] = SavedTools[o]; SavedTools[o] = nil; Notify(o .. " → " .. n, Color3.fromRGB(100, 200, 180))
end)

AS(savePage, 13)
AL(savePage, "Export/Import (JSON):", 14)
local expIn = AI(savePage, "Tool name...", 15)
local expOut = AI(savePage, "Data...", 16, true)

AB(savePage, "📤 Export", 17, function()
	local n = expIn.Text; if not ActiveTools[n] then Notify("Not found!") return end
	local t = ActiveTools[n]
	expOut.Text = HttpService:JSONEncode({v = "1.0", n = t.name, cd = t.cooldown, d = t.desc, co = t.condition, ch = t.chain, ev = t.event, c = t.code})
	Notify("Exported!", Color3.fromRGB(100, 180, 255))
end)

AB(savePage, "📥 Import", 18, function()
	local raw = expOut.Text; if raw == "" then Notify("Paste!") return end
	local ok, d = pcall(function() return HttpService:JSONDecode(raw) end)
	if ok and d and d.n then SpawnTool(d.n, d.cd or "0", d.c or "", d.d or "", d.co or "", d.ch or "", d.ev or ""); Notify("Imported: " .. d.n, Color3.fromRGB(60, 180, 120))
	else Notify("Invalid!", Color3.fromRGB(200, 60, 60)) end
end)

AB(savePage, "📤 Export ALL", 19, function()
	local all = {}; for _, v in pairs(ActiveTools) do table.insert(all, {v = "1.0", n = v.name, cd = v.cooldown, d = v.desc, co = v.condition, ch = v.chain, ev = v.event, c = v.code}) end
	if #all == 0 then Notify("No tools!") return end
	expOut.Text = HttpService:JSONEncode(all); Notify("Exported " .. #all, Color3.fromRGB(100, 180, 255))
end)

AB(savePage, "📥 Import ALL", 20, function()
	local raw = expOut.Text; if raw == "" then Notify("Paste!") return end
	local ok, d = pcall(function() return HttpService:JSONDecode(raw) end)
	if ok and type(d) == "table" then
		local c = 0
		if d.n then SpawnTool(d.n, d.cd or "0", d.c or "", d.d or "", d.co or "", d.ch or "", d.ev or ""); c = 1
		else for _, x in pairs(d) do if x.n then SpawnTool(x.n, x.cd or "0", x.c or "", x.d or "", x.co or "", x.ch or "", x.ev or ""); c = c + 1 end end end
		Notify("Imported " .. c, Color3.fromRGB(60, 180, 120))
	else Notify("Invalid!", Color3.fromRGB(200, 60, 60)) end
end)

AS(savePage, 21)
AL(savePage, "Saved Slots:", 22)
local savesLabel = AIB(savePage, 23, 65)

AL(macroPage, "Macro Name:", 1)
local mcNameIn = AI(macroPage, "Name...", 2)
AL(macroPage, "Sequence (comma sep):", 3)
local mcSeqIn = AI(macroPage, "Tool1,Tool2...", 4)
AL(macroPage, "Delay (sec):", 5)
local mcDelIn = AI(macroPage, "0.5", 6)
AL(macroPage, "Repeat (0=inf):", 7)
local mcRepIn = AI(macroPage, "1", 8)
AS(macroPage, 9)

AB(macroPage, "▶ Create Macro", 10, function()
	local mn = mcNameIn.Text; local seq = mcSeqIn.Text; if mn == "" or seq == "" then Notify("Fill!") return end
	local tools = {}; for t in seq:gmatch("[^,]+") do table.insert(tools, t:match("^%s*(.-)%s*$")) end
	Macros[mn] = {tools = tools, delay = tonumber(mcDelIn.Text) or 0.5, repeats = tonumber(mcRepIn.Text) or 1}
	Notify("Macro: " .. mn .. " (" .. #tools .. ")", Color3.fromRGB(60, 180, 120))
end)

AB(macroPage, "▶ Run", 11, function()
	local mn = mcNameIn.Text; if not Macros[mn] then Notify("Not found!") return end
	local m = Macros[mn]; Notify("Running: " .. mn, Color3.fromRGB(255, 200, 60))
	task.spawn(function()
		local count = 0; local reps = m.repeats or 1
		while (reps == 0 or count < reps) and getgenv().ToolsCreator do
			for i, tn in ipairs(m.tools) do
				if ActiveTools[tn] then pcall(function() local env = setmetatable({vars = ToolVars, player = Player}, {__index = getfenv()}); setfenv(loadstring(ActiveTools[tn].code), env)() end) end
				if i < #m.tools then task.wait(m.delay) end
			end
			count = count + 1
			if reps == 0 and not getgenv()["macro_loop_" .. mn] then break end
			if count < reps or reps == 0 then task.wait(m.delay) end
		end
		Notify("Done: " .. mn, Color3.fromRGB(60, 180, 120))
	end)
end)

AB(macroPage, "🔁 Loop", 12, function()
	local mn = mcNameIn.Text; if not Macros[mn] then Notify("Not found!") return end
	if getgenv()["macro_loop_" .. mn] then getgenv()["macro_loop_" .. mn] = false; Notify("Stopped: " .. mn); return end
	getgenv()["macro_loop_" .. mn] = true; Notify("Looping: " .. mn, Color3.fromRGB(255, 200, 60))
	task.spawn(function()
		while getgenv()["macro_loop_" .. mn] and Macros[mn] do
			for i, tn in ipairs(Macros[mn].tools) do
				if not getgenv()["macro_loop_" .. mn] then break end
				if ActiveTools[tn] then pcall(function() local env = setmetatable({vars = ToolVars, player = Player}, {__index = getfenv()}); setfenv(loadstring(ActiveTools[tn].code), env)() end) end
				if i < #Macros[mn].tools then task.wait(Macros[mn].delay) end
			end
			task.wait(Macros[mn].delay)
		end
	end)
end)

AB(macroPage, "🗑 Delete", 13, function()
	local mn = mcNameIn.Text; if not Macros[mn] then Notify("Not found!") return end
	getgenv()["macro_loop_" .. mn] = false; Macros[mn] = nil; Notify("Deleted: " .. mn, Color3.fromRGB(200, 120, 60))
end)

AS(macroPage, 14)
AL(macroPage, "Macros:", 15)
local macroLabel = AIB(macroPage, 16, 60)
AL(macroPage, "Combo History:", 17)
local comboLabel = AIB(macroPage, 18, 50)

AL(groupPage, "Group Name:", 1)
local grpIn = AI(groupPage, "Group...", 2)
AL(groupPage, "Tools (comma sep):", 3)
local grpTIn = AI(groupPage, "Tool1,Tool2...", 4)
AS(groupPage, 5)

AB(groupPage, "📁 Create", 6, function()
	local gn = grpIn.Text; local gt = grpTIn.Text; if gn == "" or gt == "" then Notify("Fill!") return end
	local tools = {}; for t in gt:gmatch("[^,]+") do table.insert(tools, t:match("^%s*(.-)%s*$")) end
	ToolGroups[gn] = tools; Notify("Group: " .. gn .. " (" .. #tools .. ")", Color3.fromRGB(60, 180, 120))
end)

AB(groupPage, "▶ Equip", 7, function()
	local gn = grpIn.Text; if not ToolGroups[gn] then Notify("Not found!") return end
	local char = Player.Character; if not char then return end
	local hum = char:FindFirstChildOfClass("Humanoid"); if not hum then return end
	for _, tn in ipairs(ToolGroups[gn]) do
		local tool = Player:FindFirstChild("Backpack") and Player.Backpack:FindFirstChild(tn)
		if not tool then tool = char:FindFirstChild(tn) end
		if tool and tool:IsA("Tool") then hum:EquipTool(tool) end
	end
	Notify("Equipped: " .. gn, Color3.fromRGB(100, 180, 255))
end)

AB(groupPage, "▶ Run All Scripts", 8, function()
	local gn = grpIn.Text; if not ToolGroups[gn] then Notify("Not found!") return end
	local c = 0
	for _, tn in ipairs(ToolGroups[gn]) do
		if ActiveTools[tn] then pcall(function() local env = setmetatable({vars = ToolVars, player = Player}, {__index = getfenv()}); setfenv(loadstring(ActiveTools[tn].code), env)() end); c = c + 1 end
	end
	Notify("Ran " .. c, Color3.fromRGB(100, 180, 255))
end)

AB(groupPage, "🗑 Delete Group", 9, function()
	local gn = grpIn.Text; if not ToolGroups[gn] then Notify("Not found!") return end
	ToolGroups[gn] = nil; Notify("Deleted: " .. gn, Color3.fromRGB(200, 120, 60))
end)

AB(groupPage, "🗑 Delete + Tools", 10, function()
	local gn = grpIn.Text; if not ToolGroups[gn] then Notify("Not found!") return end
	for _, tn in ipairs(ToolGroups[gn]) do if ActiveTools[tn] then DestroyTool(tn) end end
	ToolGroups[gn] = nil; Notify("Deleted all: " .. gn, Color3.fromRGB(200, 120, 60))
end)

AS(groupPage, 11)
AL(groupPage, "Groups:", 12)
local grpLabel = AIB(groupPage, 13, 65)

AL(varPage, "Variable Name:", 1)
local varNameIn = AI(varPage, "var_name", 2)
AL(varPage, "Value:", 3)
local varValIn = AI(varPage, "value", 4)
AS(varPage, 5)

AB(varPage, "📊 Set", 6, function()
	local vn = varNameIn.Text; local vv = varValIn.Text; if vn == "" then return end
	local num = tonumber(vv)
	if num then ToolVars[vn] = num elseif vv == "true" then ToolVars[vn] = true elseif vv == "false" then ToolVars[vn] = false elseif vv == "nil" then ToolVars[vn] = nil else ToolVars[vn] = vv end
	Notify("Set: " .. vn .. " = " .. tostring(ToolVars[vn]), Color3.fromRGB(60, 180, 120))
end)

AB(varPage, "🗑 Delete", 7, function()
	local vn = varNameIn.Text; if vn == "" then return end; ToolVars[vn] = nil; Notify("Deleted: " .. vn, Color3.fromRGB(200, 120, 60))
end)

AB(varPage, "🗑 Clear All", 8, function()
	ToolVars = {}; getgenv().TC_Vars = ToolVars; Notify("Cleared!", Color3.fromRGB(200, 120, 60))
end)

AS(varPage, 9)
AL(varPage, "Variables:", 10)
local varLabel = AIB(varPage, 11, 80)
AL(varPage, "Usage:", 12)
local varHelp = AIB(varPage, 13, 45)
varHelp.Text = "Access: vars.name\nCondition: vars.hp > 50\nShared across all tools"

AL(snippetPage, "Snippet Name:", 1)
local snipNameIn = AI(snippetPage, "Name...", 2)
AL(snippetPage, "Code:", 3)
local snipCodeIn = AI(snippetPage, "Reusable code...", 4, true)
AS(snippetPage, 5)

AB(snippetPage, "✂ Save", 6, function()
	local sn = snipNameIn.Text; local sc = snipCodeIn.Text; if sn == "" or sc == "" then Notify("Fill!") return end
	Snippets[sn] = sc; Notify("Saved: " .. sn, Color3.fromRGB(60, 180, 120))
end)

AB(snippetPage, "📋 Load to Editor", 7, function()
	local sn = snipNameIn.Text; if not Snippets[sn] then Notify("Not found!") return end
	codeIn.Text = Snippets[sn]; SwitchTab("Create"); Notify("Loaded: " .. sn)
end)

AB(snippetPage, "📋 Append", 8, function()
	local sn = snipNameIn.Text; if not Snippets[sn] then Notify("Not found!") return end
	codeIn.Text = codeIn.Text .. "\n" .. Snippets[sn]; Notify("Appended: " .. sn)
end)

AB(snippetPage, "⚡ Run", 9, function()
	local sn = snipNameIn.Text; if not Snippets[sn] then Notify("Not found!") return end
	local ok, err = pcall(function() local env = setmetatable({vars = ToolVars, player = Player}, {__index = getfenv()}); setfenv(loadstring(Snippets[sn]), env)() end)
	Notify(ok and "OK!" or ("Err: " .. tostring(err):sub(1, 30)), ok and Color3.fromRGB(60, 180, 120) or Color3.fromRGB(200, 60, 60))
end)

AB(snippetPage, "🗑 Delete", 10, function()
	local sn = snipNameIn.Text; if not Snippets[sn] then Notify("Not found!") return end
	Snippets[sn] = nil; Notify("Deleted: " .. sn, Color3.fromRGB(200, 120, 60))
end)

AS(snippetPage, 11)
AL(snippetPage, "Snippets:", 12)
local snipLabel = AIB(snippetPage, 13, 70)

AL(profilePage, "Profile:", 1)
local profIn = AI(profilePage, "Name...", 2)
AS(profilePage, 3)

AB(profilePage, "👤 Save", 4, function()
	local pn = profIn.Text; if pn == "" then Notify("Enter name!") return end
	local tools = {}; for _, v in pairs(ActiveTools) do table.insert(tools, {name = v.name, cooldown = v.cooldown, code = v.code, desc = v.desc, condition = v.condition, chain = v.chain, event = v.event}) end
	local kbs = {}; for k, v in pairs(Keybinds) do kbs[k] = v end
	local mcs = {}; for k, v in pairs(Macros) do mcs[k] = v end
	local gs = {}; for k, v in pairs(ToolGroups) do gs[k] = v end
	local vs = {}; for k, v in pairs(ToolVars) do vs[k] = v end
	local sn = {}; for k, v in pairs(Snippets) do sn[k] = v end
	Profiles[pn] = {tools = tools, keybinds = kbs, macros = mcs, groups = gs, vars = vs, snippets = sn}
	currentProfile = pn; Notify("Saved: " .. pn, Color3.fromRGB(60, 180, 120))
end)

AB(profilePage, "👤 Load", 5, function()
	local pn = profIn.Text; if not Profiles[pn] then Notify("Not found!") return end
	local p = Profiles[pn]
	local ns = {}; for n in pairs(ActiveTools) do table.insert(ns, n) end
	for _, n in ipairs(ns) do DestroyTool(n) end; ActiveTools = {}
	if p.tools then for _, d in pairs(p.tools) do SpawnTool(d.name, d.cooldown, d.code, d.desc, d.condition, d.chain, d.event) end end
	if p.keybinds then Keybinds = p.keybinds; getgenv().TC_Keybinds = Keybinds end
	if p.macros then Macros = p.macros; getgenv().TC_Macros = Macros end
	if p.groups then ToolGroups = p.groups; getgenv().TC_Groups = ToolGroups end
	if p.vars then ToolVars = p.vars; getgenv().TC_Vars = ToolVars end
	if p.snippets then Snippets = p.snippets; getgenv().TC_Snippets = Snippets end
	currentProfile = pn; Notify("Loaded: " .. pn, Color3.fromRGB(100, 180, 255))
end)

AB(profilePage, "🗑 Delete", 6, function()
	local pn = profIn.Text; if not Profiles[pn] then Notify("Not found!") return end
	Profiles[pn] = nil; Notify("Deleted: " .. pn, Color3.fromRGB(200, 120, 60))
end)

AB(profilePage, "📤 Export JSON", 7, function()
	local pn = profIn.Text; if not Profiles[pn] then Notify("Not found!") return end
	expOut.Text = HttpService:JSONEncode({profile = pn, data = Profiles[pn]})
	SwitchTab("Saves"); Notify("Exported!", Color3.fromRGB(100, 180, 255))
end)

AS(profilePage, 8)
AL(profilePage, "Profiles:", 9)
local profLabel = AIB(profilePage, 10, 65)

AL(logPage, "Log:", 1)
local logLabel = AIB(logPage, 2, 200)

AB(logPage, "🗑 Clear", 3, function() LogEntries = {}; Notify("Cleared!") end)

AB(logPage, "📋 Copy", 4, function()
	if setclipboard then setclipboard(table.concat(LogEntries, "\n")); Notify("Copied!") else Notify("No clipboard!") end
end)

AS(logPage, 5)
AL(logPage, "Stats:", 6)
local statsLabel = AIB(logPage, 7, 110)

AL(configPage, "⚙ Settings:", 1)
AS(configPage, 2)
ATog(configPage, "Auto Re-equip", 3, true, function(v) autoRespawnTools = v end)
ATog(configPage, "Logging", 4, true, function(v) logEnabled = v end)
ATog(configPage, "Cooldown Bar", 5, true, function(v) cdOverlayEnabled = v; if not v then CdOverlay.Visible = false end end)
ATog(configPage, "Particles/Shooting Stars", 6, true, function(v) particleEnabled = v end)
ATog(configPage, "CD Name Change", 7, true, function(v) cdNamingEnabled = v end)
ATog(configPage, "Auto Clear Dupes", 8, true, function(v) autoClearDupes = v end)

AS(configPage, 9)
AL(configPage, "Opacity (0-100):", 10)
local opIn = AI(configPage, "100", 11)
AB(configPage, "Apply", 12, function()
	local v = tonumber(opIn.Text); if not v then return end
	GradBG.BackgroundTransparency = 1 - math.clamp(v, 0, 100) / 100; Notify("Opacity: " .. math.floor(v) .. "%")
end)

AL(configPage, "Scale (50-200):", 13)
local scIn = AI(configPage, "100", 14)
AB(configPage, "Apply", 15, function()
	local v = tonumber(scIn.Text); if not v then return end
	v = math.clamp(v, 50, 200) / 100
	fullSize = UDim2.new(0, math.floor(248 * v), 0, math.floor(370 * v))
	miniSize = UDim2.new(0, math.floor(248 * v), 0, 24)
	MainFrame.Size = minimized and miniSize or fullSize; Notify("Scale: " .. math.floor(v * 100) .. "%")
end)

AS(configPage, 16)

local themeColors = {
	Galaxy = {bg = Color3.fromRGB(5, 1, 15), title = Color3.fromRGB(10, 3, 26)},
	Nebula = {bg = Color3.fromRGB(15, 1, 10), title = Color3.fromRGB(28, 3, 18)},
	Void = {bg = Color3.fromRGB(1, 1, 5), title = Color3.fromRGB(4, 4, 14)},
	Cosmic = {bg = Color3.fromRGB(8, 6, 1), title = Color3.fromRGB(18, 14, 4)},
	Blood = {bg = Color3.fromRGB(12, 1, 1), title = Color3.fromRGB(25, 3, 3)},
	Emerald = {bg = Color3.fromRGB(1, 10, 5), title = Color3.fromRGB(3, 22, 12)},
	Frost = {bg = Color3.fromRGB(2, 8, 14), title = Color3.fromRGB(5, 16, 28)}
}

ADrop(configPage, "Theme:", {"Galaxy","Nebula","Void","Cosmic","Blood","Emerald","Frost"}, 17, function(theme)
	local t = themeColors[theme]
	Tween(GradBG, {BackgroundColor3 = t.bg}, 0.4)
	Tween(TitleBar, {BackgroundColor3 = t.title}, 0.4)
	Notify("Theme: " .. theme)
end)

AS(configPage, 18)
AB(configPage, "🔴 Destroy GUI", 19, function()
	getgenv().ToolsCreator = false
	for n, c in pairs(Connections) do c:Disconnect() end
	for tn, evts in pairs(ToolEvents) do for _, c in pairs(evts) do pcall(function() c:Disconnect() end) end end
	for tn in pairs(ToolSchedules) do ToolSchedules[tn] = nil end
	for mn in pairs(Macros) do getgenv()["macro_loop_" .. mn] = false end
	Connections = {}; ToolEvents = {}; ScreenGui:Destroy()
end, Color3.fromRGB(80, 10, 10))

AS(configPage, 20)
AL(configPage, "V1.0 | Tools Creator", 21)

SwitchTab("Create")

ToggleBtn.MouseButton1Click:Connect(function()
	guiOpen = not guiOpen
	if guiOpen then
		MainFrame.Visible = true
		MainFrame.Size = UDim2.new(0, fullSize.X.Offset, 0, 0)
		MainFrame.BackgroundTransparency = 1
		Tween(MainFrame, {Size = minimized and miniSize or fullSize, BackgroundTransparency = 0}, 0.3)
		Tween(ToggleBtn, {BackgroundColor3 = Color3.fromRGB(38, 14, 85)}, 0.2)
		ToggleBtn.Text = "✦"
	else
		Tween(MainFrame, {Size = UDim2.new(0, fullSize.X.Offset, 0, 0), BackgroundTransparency = 1}, 0.25)
		Tween(ToggleBtn, {BackgroundColor3 = Color3.fromRGB(8, 2, 20)}, 0.2)
		ToggleBtn.Text = "★"
		task.delay(0.26, function() if not guiOpen then MainFrame.Visible = false end end)
	end
end)

UIS.InputBegan:Connect(function(input, gpe)
	if gpe then return end
	if input.UserInputType == Enum.UserInputType.Keyboard then
		local key = input.KeyCode.Name:upper()
		if Keybinds[key] and ActiveTools[Keybinds[key]] then
			local tn = Keybinds[key]; if ToolCooldowns[tn] then return end
			local a = ActiveTools[tn]; local cond = a.condition or ""
			if cond ~= "" then
				local ok, r = pcall(function() local env = setmetatable({vars = ToolVars, player = Player}, {__index = getfenv()}); return setfenv(loadstring("return " .. cond), env)() end)
				if not ok or not r then return end
			end
			ToolCooldowns[tn] = true
			if ToolStats[tn] then ToolStats[tn].uses = ToolStats[tn].uses + 1; ToolStats[tn].lastUsed = os.date("%H:%M:%S") end
			pcall(function() local env = setmetatable({vars = ToolVars, player = Player}, {__index = getfenv()}); setfenv(loadstring(a.code), env)() end)
			AddLog("Key [" .. key .. "] → " .. tn)
			local cd = tonumber(a.cooldown) or 0
			if cd > 0 then
				ToolCdConns[tn] = true
				task.spawn(function()
					local st = tick()
					while tick() - st < cd and ToolCdConns[tn] do
						UpdateToolCdName(tn, cd - (tick() - st))
						task.wait(0.05)
					end
					ToolCooldowns[tn] = false
					ToolCdConns[tn] = nil
					UpdateToolCdName(tn, nil)
				end)
			else
				ToolCooldowns[tn] = false
			end
		end
	end
end)

local bpConn
local charConn

local function WatchBackpack()
	local bp = Player:FindFirstChild("Backpack")
	if not bp then return end
	if bpConn then bpConn:Disconnect() end
	bpConn = bp.ChildAdded:Connect(function(child)
		if not child:IsA("Tool") then return end
		if not autoClearDupes then return end
		task.wait()
		for tn, myTool in pairs(ToolInstances) do
			if child.Name == tn and child ~= myTool and myTool and myTool.Parent then
				child:Destroy()
				AddLog("Auto-removed dupe: " .. tn)
			end
		end
	end)
end

local function WatchChar()
	local char = Player.Character
	if not char then return end
	if charConn then charConn:Disconnect() end
	charConn = char.ChildAdded:Connect(function(child)
		if not child:IsA("Tool") then return end
		if not autoClearDupes then return end
		task.wait()
		for tn, myTool in pairs(ToolInstances) do
			if child.Name == tn and child ~= myTool and myTool and myTool.Parent then
				child:Destroy()
				AddLog("Auto-removed dupe (char): " .. tn)
			end
		end
	end)
end

WatchBackpack()
WatchChar()

task.spawn(function()
	while getgenv().ToolsCreator and ScreenGui.Parent do
		local tc = 0; for _ in pairs(ActiveTools) do tc = tc + 1 end
		CountLabel.Text = "[" .. tc .. "]"

		local favs, others = {}, {}
		for name, v in pairs(ActiveTools) do
			local cdText = ""
			if ToolCooldowns[name] and ToolTimers[name] then
				local rem = ToolTimers[name].duration - (tick() - ToolTimers[name].start)
				if rem > 0 then cdText = "⏳" .. string.format("%.1f", rem) else cdText = "✓" end
			else cdText = "✓" end
			local fav = Favorites[name] and "⭐" or ""
			local uses = ToolStats[name] and ToolStats[name].uses or 0
			local streak = ToolStats[name] and ToolStats[name].streak or 0
			local ev = (v.event and v.event ~= "") and "🔔" or ""
			local ch = (v.chain and v.chain ~= "") and "🔗" or ""
			local line = cdText .. fav .. ev .. ch .. " " .. name .. " [" .. v.cooldown .. "s] x" .. uses
			if streak > 1 then line = line .. " 🔥" .. streak end
			if Favorites[name] then table.insert(favs, line) else table.insert(others, line) end
		end
		local at = {}; for _, l in ipairs(favs) do table.insert(at, l) end; for _, l in ipairs(others) do table.insert(at, l) end
		activeLabel.Text = #at > 0 and table.concat(at, "\n") or "No active tools"

		local kbs = {}; for k, t in pairs(Keybinds) do table.insert(kbs, "[" .. k .. "] → " .. t) end
		kbLabel.Text = #kbs > 0 and table.concat(kbs, "\n") or "No keybinds"

		local sv = {}; for name, data in pairs(SavedTools) do
			local ns = {}; for _, t in pairs(data) do table.insert(ns, t.name) end
			table.insert(sv, "📁 " .. name .. " (" .. #data .. "): " .. table.concat(ns, ", "))
		end
		savesLabel.Text = #sv > 0 and table.concat(sv, "\n") or "No saves"

		logLabel.Text = #LogEntries > 0 and table.concat(LogEntries, "\n") or "No entries"

		local mc = {}; for mn, m in pairs(Macros) do
			local lp = getgenv()["macro_loop_" .. mn] and " 🔁" or ""
			table.insert(mc, "▶ " .. mn .. " (" .. #m.tools .. ", " .. m.delay .. "s x" .. (m.repeats or 1) .. ")" .. lp)
		end
		macroLabel.Text = #mc > 0 and table.concat(mc, "\n") or "No macros"

		local cb = {}; local now = tick()
		for i, c in ipairs(ComboTracker) do
			if i > 8 then break end
			table.insert(cb, c.name .. " (" .. string.format("%.1f", now - c.time) .. "s)")
		end
		comboLabel.Text = #cb > 0 and table.concat(cb, "\n") or "No combos"

		local gps = {}; for gn, gt in pairs(ToolGroups) do table.insert(gps, "📁 " .. gn .. ": " .. table.concat(gt, ", ")) end
		grpLabel.Text = #gps > 0 and table.concat(gps, "\n") or "No groups"

		local vrs = {}; for k, v in pairs(ToolVars) do table.insert(vrs, k .. " = " .. tostring(v) .. " (" .. type(v) .. ")") end
		varLabel.Text = #vrs > 0 and table.concat(vrs, "\n") or "No variables"

		local sps = {}; for k in pairs(Snippets) do table.insert(sps, "✂ " .. k) end
		snipLabel.Text = #sps > 0 and table.concat(sps, "\n") or "No snippets"

		local pfs = {}; for k, p in pairs(Profiles) do
			local ptc = p.tools and #p.tools or 0
			table.insert(pfs, "👤 " .. k .. " (" .. ptc .. ")" .. (k == currentProfile and " ◄" or ""))
		end
		profLabel.Text = #pfs > 0 and table.concat(pfs, "\n") or "No profiles"

		local totalU, totalE, totalT = 0, 0, 0
		for _, s in pairs(ToolStats) do totalU = totalU + s.uses; totalE = totalE + s.errors; totalT = totalT + s.totalTime end
		local sc2 = 0; for _ in pairs(SavedTools) do sc2 = sc2 + 1 end
		local mc2 = 0; for _ in pairs(Macros) do mc2 = mc2 + 1 end
		local gc2 = 0; for _ in pairs(ToolGroups) do gc2 = gc2 + 1 end
		local vc2 = 0; for _ in pairs(ToolVars) do vc2 = vc2 + 1 end
		local sc3 = 0; for _ in pairs(Snippets) do sc3 = sc3 + 1 end
		local pc2 = 0; for _ in pairs(Profiles) do pc2 = pc2 + 1 end
		local elapsed = tick() - sessionStart
		local mins = math.floor(elapsed / 60)
		local secs = math.floor(elapsed % 60)
		local st = {
			"Tools:" .. tc .. " Saves:" .. sc2 .. " Macros:" .. mc2,
			"Groups:" .. gc2 .. " Vars:" .. vc2 .. " Snips:" .. sc3,
			"Profiles:" .. pc2 .. " Active:" .. currentProfile,
			"Uses:" .. totalU .. " Errors:" .. totalE,
			"Exec:" .. string.format("%.2f", totalT) .. "s",
			"Undo:" .. #UndoHistory .. " KB:" .. #kbs,
			"Created:" .. toolCreationCount .. " Session:" .. mins .. "m" .. secs .. "s"
		}
		local topN, topU = "N/A", 0
		for n, s in pairs(ToolStats) do if s.uses > topU then topU = s.uses; topN = n end end
		if topU > 0 then table.insert(st, "Top: " .. topN .. " (x" .. topU .. ")") end
		local topS, topSN = 0, "N/A"
		for n, s in pairs(ToolStats) do if s.bestStreak > topS then topS = s.bestStreak; topSN = n end end
		if topS > 1 then table.insert(st, "Streak: " .. topSN .. " (x" .. topS .. ")") end
		statsLabel.Text = table.concat(st, "\n")

		if autoClearDupes then ScanAndClearAllDupes() end

		task.wait(0.4)
	end
end)

Player.CharacterAdded:Connect(function(char)
	task.wait(1)
	WatchBackpack()
	WatchChar()
	if not autoRespawnTools then return end
	local d = {}; for _, v in pairs(ActiveTools) do table.insert(d, {name = v.name, cooldown = v.cooldown, code = v.code, desc = v.desc, condition = v.condition, chain = v.chain, event = v.event}) end
	for _, x in pairs(d) do SpawnTool(x.name, x.cooldown, x.code, x.desc, x.condition, x.chain, x.event) end
	if #d > 0 then AddLog("Respawn: " .. #d .. " tools") end
end)

AddLog("Tools Creator V1.0 initialized")
Notify("✦ Tools Creator V1.0!", Color3.fromRGB(155, 105, 250))
